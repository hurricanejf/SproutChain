export default function Footer() {
  return (
    <footer className="text-center text-zinc-500 text-sm py-8">
      SproutChain © {new Date().getFullYear()} — Growing the future.
    </footer>
  );
}
